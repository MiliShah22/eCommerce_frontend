# TODO

- [x] Update `frontend/src/app/product/[id]/page.tsx` to always apply default selections (color/size) when user doesn’t select.
- [ ] Extend product variant handling for “other option” per service (food: cheese/sauce/beverages) by adding support to data model and UI (if already present); otherwise implement infrastructure.
- [ ] Update cart variant display if new variant fields are introduced.
- [ ] Run lint/build to ensure no TS errors (current build error is unrelated: missing `getStatusStyle`).


